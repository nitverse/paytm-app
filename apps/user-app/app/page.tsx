"use client";
import React from 'react';
import {useBalance} from "@repo/store/useBalance"
const page = () => {
  const balance = useBalance();
  return (
    <div>

    </div>
  )
}

export default page